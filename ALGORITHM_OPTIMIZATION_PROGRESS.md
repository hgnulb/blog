# 算法文章优化进度

> 最后更新：2026-09-04 03:57:01 CST  
> 仓库：`/Users/libin/WebstormProjects/myblog`  
> 用途：新会话开始前先读取本文件，按记录的分工和断点继续；每完成一批后更新。

## 会话与分工

| 工作流                 | Codex 会话 / Thread ID                 | 负责范围                        |               批次大小 | 状态               |
| ---------------------- | -------------------------------------- | ------------------------------- | ---------------------: | ------------------ |
| 高频工作流（当前会话） | `01a067df-0af8-7282-847c-e4bea4b3bd95` | `frequency >= 2`，严格从高到低  | 20 篇，10 agent × 2 篇 | 已暂停，等待“继续” |
| 低频工作流（另一会话） | 待该会话补充                           | `frequency = 1`，完成后处理 `0` |           由该会话维护 | 进行中             |

两个会话共享同一个工作区，必须保持范围互斥。禁止在优化过程中执行 `git switch`、`pull`、`stash`、`reset`、`commit` 或 `push`。

## 当前断点

| 指标                                           | 当前值 |
| ---------------------------------------------- | -----: |
| 含 `frequency` 的文章总数                      |   1389 |
| 当前工作区已修改文章                           |    682 |
| 全部剩余                                       |    707 |
| 高频分区总数                                   |    840 |
| 高频分区已修改                                 |    482 |
| 高频分区剩余                                   |    358 |
| 低频分区总数                                   |    549 |
| 低频分区已修改（工作区观察值，需低频会话确认） |    200 |
| 低频分区剩余（工作区观察值）                   |    349 |

高频工作流下一个文件：

```text
frequency: 5
_posts/2026-03-28-LeetCode 393. UTF-8 编码验证.md
```

## 当前批次状态

- 状态：20 篇全部完成并验证，已暂停
- 范围：`frequency 5 → 5`
- 数量：20 篇，10 agent × 2 篇
- 起点：剑指 Offer 58-I《翻转单词顺序》
- 终点：387《字符串中的第一个唯一字符》
- 下一批：尚未启动；只有用户明确说“继续”后才能开始

说明：高频分区中有 2 篇是任务开始前已有的修改（LeetCode 2、LeetCode 8），本工作流未覆盖它们；其余 480 篇由本工作流完成。

## 新会话续跑方式

在仓库 `/Users/libin/WebstormProjects/myblog` 中开启新会话后，发送：

```text
继续“优化算法题解内容结构”。先完整读取 AGENTS.md、CLAUDE.md 和 ALGORITHM_OPTIMIZATION_PROGRESS.md，严格从记录的断点开始，只完成下一批 20 篇。完成改写、Java/Go 编译与样例校验、结构/公式/保护区检查、Jekyll 非生产构建，并更新整体进度。完成本批后立即停止；以后每批必须等我明确说“继续”才能启动。不要加载 _config_prod.yml，不要提交或推送，也不要处理低频工作流的范围。
```

不需要恢复旧会话；本文件是跨会话的唯一进度断点。若用户没有明确说“继续”，不得启动新批次。

## 最近完成批次

- 范围：`frequency 5 → 5`
- 数量：20 篇
- 变更：新增 773 行、删除 1087 行，净减少 314 行
- 验证：40 个 Java/Go 代码块按自身 imports 编译并通过关键样例；结构、公式和保护区检查通过
- 重点校验：双向扫描、单调队列、前缀和、BFS/DFS、动态规划、贪心、二分和稀疏矩阵乘法
- 构建：`bundle exec jekyll build --config _config.yml --incremental` 在独立临时目录通过

## 内容规范

只重点优化以下章节：

1. 核心思路
2. 解题步骤
3. 复杂度分析
4. 关键点总结
5. 易错点总结
6. 代码实现

要求：

- 解法适合面试口述，按“问题关键 → 选择依据 → 状态或不变量 → 正确性 → 执行步骤 → 边界反例”组织。
- 原则上只保留一个面试主解法；重要替代方案只简述取舍，不堆重复实现。
- 算法题保留一份 Java 和一份 Go；代码块必须包含自身需要的 imports，并通过编译和关键样例。
- SQL 等非 Java/Go 题保留题目原生语言，不强行增加 Java/Go。
- 不修改目录、文件名、frontmatter 和“相似题目”；题目描述或题意分析仅在存在事实错误或统一格式问题时做最小修正，并在汇总中单列。
- 数学公式内禁止裸 `|`。绝对值使用 `\lvert ... \rvert`，其他竖线使用 Markdown 安全转义，避免 Kramdown 误解析成表格。
- 不加载 `_config_prod.yml`。

## 每批执行流程

1. 重新读取本文件和 `CLAUDE.md`。
2. 读取当前 Git 改动，只在所属 `frequency` 分区内选取未处理文章。
3. 按 `frequency` 降序、同频按文件名排序。
4. 高频工作流每批只选 20 篇，启动 10 个 agent，每个恰好负责 2 篇，文件不得重叠。
5. agent 完成自身结构、公式、编译和关键样例检查。
6. 主控统一检查章节唯一性、代码围栏、公式竖线、前置保护区和“相似题目”保护区。
7. 主控按代码块自身 imports 编译；只对复杂题或校验异常做交叉复核。
8. 使用独立临时输出目录运行 Jekyll 非生产构建。
9. 当前 20 篇全部完成改写、校验、汇总并更新本文件后，才能选择和启动下一批；禁止提前铺开后续文章。
10. 每批完成后立即停止；只有用户明确说“继续”后，才能启动下一批。

## 全局格式清扫

最初发现 75 篇存在数学公式裸竖线。已修复已处理文章，并把规则加入后续批次；当前扫描仍有 52 篇、381 个裸竖线待处理。为避免尚未优化的文章因格式改动被误判为“已完成”，剩余内容随各自算法批次修复，所有文章处理完后再做一次全库清扫。

## Git 状态约定

- 当前开发分支：`main`，基线提交 `a839258`
- pull 前的干净备份分支：`feature-20260904`，指向 `306b775`
- `feature-20260904` 不包含任何本次未提交修改
- 所有文章优化继续保留在 `main` 工作区，不暂存、不提交、不推送
